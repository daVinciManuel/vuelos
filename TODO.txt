# por hacer:
+ formulario de login
+ connect.php
+ encrypt passwords
- mlogin.php [ fn: getPasswordOf ]
+ GESTION DE $_SESSION['user']
+ inicio.php
+ activar/desactivar envio de formulario

- vuelos[
    v[flight_id,
      flightno,
      airline_id,
      from,
      to,
      departure,
      arrival,
      prize],
    v[],
    v[],
    v[],
    ...
  ]

